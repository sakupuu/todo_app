<%= link_to "New Todo Item", new_todo_path %>
<h1> Index Page! </h1>
<% @todos.each do |todo| %>
  <li><%=todo.title%></li>
<% end %>

<% @todos.each do |todo| %>
  <li> <%= link_to todo.title, todo_path(todo.id) %></li>
<% end %>
